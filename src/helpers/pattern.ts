// eslint-disable-next-line no-useless-escape
const patternEmail = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/g;

// eslint-disable-next-line no-useless-escape
const patternPhone = /^[\+]{0,1}380([0-9]{9})$/g;

export { patternEmail, patternPhone };
